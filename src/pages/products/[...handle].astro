---
import BaseLayout from "../../layouts/BaseLayout.astro";
import NotFoundLayout from "../../layouts/NotFoundLayout.astro";

// Stub until Week 1: returns empty — full SSG implementation requires Shopify token.
// Worker's not_found_handling will serve 404.html for any /products/* miss.
export function getStaticPaths() {
  return [];
}

const { handle } = Astro.params;
---

<NotFoundLayout />
